interface:
  display_name: "Claude Code"
  short_description: "Repo-specific patterns and workflows for claude-code"
  default_prompt: "Use the claude-code repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true